<!DOCTYPE html>
<html lang="pt">
<head>
    <title><%= playerData ? `Perfil de ${playerData.cleanNickname}` : 'Procurar Jogador' %> | Craftsapiens</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <style>
        /* --- ESTILOS GERAIS (Baseado no Dark Mode do Discord) --- */
        body {
            background-color: #23272A;
            color: #FFFFFF;
            font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', sans-serif;
            overflow-x: hidden;
        }

        /* --- SEARCH BAR --- */
        .search-wrapper {
            background-color: #2C2F33;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0,0,0,0.1);
            margin-bottom: 30px;
            border: 1px solid #444;
        }

        .form-control-dark {
            background-color: #23272A;
            border: 1px solid #23272A;
            color: white;
            padding: 12px;
        }
        .form-control-dark:focus {
            background-color: #000;
            color: white;
            border-color: #7289DA;
            box-shadow: none;
        }

        /* --- LAYOUT DO PERFIL --- */
        .profile-container {
            display: flex;
            flex-wrap: wrap;
            gap: 25px;
            justify-content: center;
            align-items: flex-start;
            max-width: 1100px;
            margin: 0 auto;
            opacity: 0; /* Para animação de entrada */
            animation: fadeInUp 0.6s forwards;
        }

        /* --- CARTÃO DE IDENTIDADE (ESQUERDA) --- */
        .identity-card {
            background-color: #2C2F33;
            border-radius: 12px;
            width: 340px;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0,0,0,0.3);
            text-align: center;
            position: relative;
            padding-bottom: 25px;
            transition: transform 0.3s ease;
        }
        .identity-card:hover { transform: translateY(-5px); }

        .accent-header {
            height: 120px;
            width: 100%;
            /* O background-color é definido inline pelo EJS */
            clip-path: polygon(0 0, 100% 0, 100% 80%, 0 100%);
        }

        .skin-avatar {
            width: 160px;
            height: 160px;
            margin-top: -90px; /* Puxa para cima da header */
            filter: drop-shadow(0 5px 15px rgba(0,0,0,0.5));
            transition: transform 0.3s;
            position: relative;
            z-index: 2;
        }
        .skin-avatar:hover { transform: scale(1.05) rotate(-5deg); }

        .player-name {
            font-size: 24px;
            font-weight: 800;
            margin-top: 15px;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
            padding: 0 10px;
        }

        .discord-pill {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            margin-top: 15px;
            background-color: rgba(0,0,0,0.2);
            padding: 6px 16px;
            border-radius: 50px;
            color: #b9bbbe;
            font-weight: 500;
            font-size: 14px;
        }

        .status-badge {
            margin-top: 20px;
            display: inline-block;
            padding: 5px 12px;
            border-radius: 4px;
            font-weight: bold;
            font-size: 13px;
            letter-spacing: 1px;
            text-transform: uppercase;
        }

        /* --- GRID DE ESTATÍSTICAS (DIREITA) --- */
        .right-column {
            flex: 1;
            min-width: 300px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
            gap: 15px;
        }

        .stat-box {
            background-color: #2C2F33;
            border-radius: 8px;
            padding: 15px;
            display: flex;
            align-items: center;
            position: relative;
            overflow: hidden;
            border-left: 4px solid transparent; /* Cor definida inline */
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
            transition: background-color 0.2s;
        }
        .stat-box:hover { background-color: #36393f; }

        .stat-icon {
            width: 42px;
            height: 42px;
            margin-right: 15px;
            opacity: 0.9;
        }

        .stat-info {
            display: flex;
            flex-direction: column;
        }
        .stat-label {
            font-size: 12px;
            text-transform: uppercase;
            color: #8e9297;
            font-weight: 700;
            margin-bottom: 2px;
        }
        .stat-value {
            font-size: 20px;
            font-weight: bold;
            color: white;
        }

        /* --- BOX DE AMIGOS --- */
        .friends-box {
            background-color: #2C2F33;
            border-radius: 8px;
            padding: 20px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.2);
        }
        .friend-chip {
            display: inline-block;
            background-color: #23272A;
            padding: 4px 10px;
            border-radius: 4px;
            margin: 3px;
            font-size: 14px;
            color: #dcddde;
            border: 1px solid #333;
        }

        /* --- ANIMAÇÕES --- */
        @keyframes fadeInUp {
            from { opacity: 0; transform: translateY(30px); }
            to { opacity: 1; transform: translateY(0); }
        }

        /* Responsividade */
        @media (max-width: 768px) {
            .profile-container { flex-direction: column; align-items: center; }
            .identity-card { width: 100%; max-width: 400px; }
            .right-column { width: 100%; }
        }
    </style>
</head>
<body>

    <%- include('../partials/navbar.ejs') %>

    <div class="container mt-5 mb-5">
        
        <div class="text-center mb-4">
            <h2 class="fw-bold"><i class="fas fa-id-card text-primary"></i> Player Info</h2>
            <p class="text-secondary">Informações em tempo real do Survival</p>
        </div>

        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="search-wrapper">
                    <form id="searchForm" class="d-flex gap-2">
                        <input type="text" id="searchInput" class="form-control form-control-dark" 
                               placeholder="Digite o Nick do jogador..." 
                               value="<%= typeof targetName !== 'undefined' ? targetName : '' %>" required>
                        <button type="submit" class="btn btn-primary fw-bold px-4">
                            <i class="fas fa-search"></i>
                        </button>
                    </form>
                </div>
            </div>
        </div>

        <% if (locals.error) { %>
            <div class="alert alert-danger text-center mx-auto shadow-sm" style="max-width: 600px; background-color: #3c1f1f; border-color: #5a2a2a; color: #ffcccc;">
                <i class="fas fa-exclamation-triangle me-2"></i> <%= error %>
            </div>
        <% } %>

        <% if (locals.playerData) { %>
            
            <div class="profile-container">
                
                <div class="identity-card">
                    <div class="accent-header" style="background-color: <%= playerData.accentColor %>;"></div>
                    
                    <img src="<%= playerData.skinUrl %>" class="skin-avatar" alt="Skin">
                    
                    <div class="player-name" style="color: <%= playerData.accentColor %>;">
                        <% if(playerData.groupDisplay) { %>
                            <span style="font-size: 0.8em; opacity: 0.9;">[<%= playerData.groupDisplay %>]</span><br>
                        <% } %>
                        <%= playerData.cleanNickname %>
                    </div>

                    <div class="discord-pill">
                        <% if (playerData.discord) { %>
                            <img src="<%= playerData.discord.avatar %>" style="width: 20px; height: 20px; border-radius: 50%;">
                            <span>@<%= playerData.discord.tag %></span>
                        <% } else { %>
                            <i class="fab fa-discord"></i> <span>Não vinculado</span>
                        <% } %>
                    </div>
                    
                    <br>

                    <div class="status-badge" 
                         style="background-color: <%= playerData.isOnline ? 'rgba(67, 181, 129, 0.2)' : 'rgba(116, 127, 141, 0.2)' %>; 
                                color: <%= playerData.isOnline ? '#43B581' : '#747F8D' %>;">
                        <i class="fas fa-circle" style="font-size: 8px; vertical-align: middle;"></i> 
                        <%= playerData.isOnline ? 'ONLINE' : 'OFFLINE' %>
                    </div>
                </div>

                <div class="right-column">
                    
                    <div class="stats-grid">
                        
                        <div class="stat-box" style="border-left-color: <%= playerData.accentColor %>;">
                            <img src="<%= icons.money %>" class="stat-icon">
                            <div class="stat-info">
                                <span class="stat-label">Dinheiro</span>
                                <span class="stat-value"><%= playerData.stats.money %></span>
                            </div>
                        </div>

                        <div class="stat-box" style="border-left-color: #FF5555;">
                            <img src="<%= icons.kills %>" class="stat-icon">
                            <div class="stat-info">
                                <span class="stat-label">Kills</span>
                                <span class="stat-value"><%= playerData.stats.kills %></span>
                            </div>
                        </div>

                        <div class="stat-box" style="border-left-color: #AAAAAA;">
                            <img src="<%= icons.deaths %>" class="stat-icon">
                            <div class="stat-info">
                                <span class="stat-label">Mortes</span>
                                <span class="stat-value"><%= playerData.stats.deaths %></span>
                            </div>
                        </div>

                        <div class="stat-box" style="border-left-color: #55FFFF;">
                            <img src="<%= icons.town %>" class="stat-icon">
                            <div class="stat-info">
                                <span class="stat-label">Cidade</span>
                                <span class="stat-value" style="font-size: 16px;"><%= playerData.stats.town %></span>
                            </div>
                        </div>

                        <div class="stat-box" style="border-left-color: #FFFF55;">
                            <img src="<%= icons.nation %>" class="stat-icon">
                            <div class="stat-info">
                                <span class="stat-label">Nação</span>
                                <span class="stat-value" style="font-size: 16px;"><%= playerData.stats.nation %></span>
                            </div>
                        </div>

                    </div> <% if (playerData.friends && playerData.friends.length > 0) { %>
                        <div class="friends-box">
                            <div class="d-flex align-items-center mb-3">
                                <img src="<%= icons.friends %>" width="24" height="24" class="me-2 opacity-75">
                                <h5 class="m-0 fw-bold" style="color: #AAAAAA; font-size: 16px;">
                                    AMIGOS (<%= playerData.friends.length %>)
                                </h5>
                            </div>
                            
                            <div>
                                <% playerData.friends.slice(0, 12).forEach(friend => { %>
                                    <span class="friend-chip"><%= friend %></span>
                                <% }); %>

                                <% if (playerData.friends.length > 12) { %>
                                    <span class="friend-chip" style="background-color: <%= playerData.accentColor %>; color: #fff; font-weight: bold;">
                                        +<%= playerData.friends.length - 12 %>
                                    </span>
                                <% } %>
                            </div>
                        </div>
                    <% } %>

                </div> </div> <% } else if (!locals.error && !locals.playerData) { %>
            <div class="text-center mt-5 opacity-50">
                <i class="fas fa-search fa-5x mb-3 text-secondary"></i>
                <h4>Pesquise um jogador acima.</h4>
                <p>Tente procurar por: <code>DG0837</code></p>
            </div>
        <% } %>

    </div>

    <script>
        document.getElementById('searchForm').addEventListener('submit', function(e) {
            e.preventDefault(); // Impede o envio tradicional
            
            var nick = document.getElementById('searchInput').value.trim();
            
            // Validação simples
            if (nick) {
                // Força a URL limpa /player/NICK
                // encodeURIComponent garante que caracteres especiais não quebrem a URL
                window.location.href = '/player/' + encodeURIComponent(nick);
            }
        });
    </script>

</body>
</html>