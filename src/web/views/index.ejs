<!DOCTYPE html>
<html lang="pt">
<head>
  <title>Início | Craftsapiens</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
  
  <style>
    body {
        background-color: #23272A;
        color: #FFFFFF;
        font-family: 'Segoe UI', 'Roboto', 'Helvetica Neue', sans-serif;
        overflow-x: hidden;
    }

    /* --- HERO SECTION --- */
    .hero-section {
        background: linear-gradient(135deg, #5865F2 0%, #4752C4 100%);
        padding: 80px 20px;
        text-align: center;
        border-radius: 0 0 30px 30px;
        box-shadow: 0 10px 30px rgba(0,0,0,0.3);
        margin-bottom: 50px;
        position: relative;
        overflow: hidden;
    }

    /* Avatar do Usuário no Hero */
    .hero-avatar {
        width: 100px;
        height: 100px;
        border-radius: 50%;
        border: 4px solid rgba(255,255,255,0.3);
        margin-bottom: 20px;
        box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        transition: transform 0.3s;
    }
    .hero-avatar:hover { transform: scale(1.1) rotate(5deg); }

    /* Barra de Pesquisa no Hero */
    .hero-search-wrapper {
        max-width: 600px;
        margin: 30px auto 0;
        position: relative;
    }
    .hero-search-input {
        background-color: rgba(255, 255, 255, 0.15);
        border: 2px solid rgba(255, 255, 255, 0.2);
        color: white;
        padding: 15px 50px 15px 25px;
        border-radius: 50px;
        width: 100%;
        font-size: 18px;
        backdrop-filter: blur(5px);
        transition: all 0.3s;
    }
    .hero-search-input::placeholder { color: rgba(255, 255, 255, 0.7); }
    .hero-search-input:focus {
        background-color: rgba(255, 255, 255, 0.25);
        border-color: white;
        outline: none;
        box-shadow: 0 0 15px rgba(255,255,255,0.2);
    }
    .hero-search-btn {
        position: absolute;
        right: 15px;
        top: 50%;
        transform: translateY(-50%);
        background: none;
        border: none;
        color: white;
        font-size: 20px;
        cursor: pointer;
        opacity: 0.8;
    }
    .hero-search-btn:hover { opacity: 1; transform: translateY(-50%) scale(1.1); }

    /* --- CARTÕES DE ACESSO RÁPIDO --- */
    .feature-card {
        background-color: #2C2F33;
        border-radius: 15px;
        padding: 30px;
        height: 100%;
        border: 1px solid #333;
        transition: transform 0.3s, background-color 0.3s, box-shadow 0.3s;
        text-decoration: none;
        display: flex;
        flex-direction: column;
        align-items: center;
        text-align: center;
        color: white;
    }

    .feature-card:hover {
        transform: translateY(-10px);
        background-color: #35393f;
        box-shadow: 0 10px 20px rgba(0,0,0,0.3);
        border-color: #5865F2;
        color: white;
    }

    .card-icon {
        font-size: 40px;
        margin-bottom: 20px;
        color: #5865F2; /* Cor Accent */
        background-color: rgba(88, 101, 242, 0.1);
        width: 80px;
        height: 80px;
        display: flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        transition: 0.3s;
    }

    .feature-card:hover .card-icon {
        background-color: #5865F2;
        color: white;
        transform: rotateY(180deg);
    }

    .card-title { font-weight: 700; font-size: 20px; margin-bottom: 10px; }
    .card-desc { font-size: 14px; color: #b9bbbe; }

    /* --- FOOTER SIMPLES --- */
    .footer-note {
        margin-top: 60px;
        text-align: center;
        color: #72767d;
        font-size: 14px;
        padding-bottom: 20px;
    }
  </style>
</head>
<body>

<%- include('../partials/navbar.ejs') %>

<header class="hero-section">
    <div class="container">
        <img src="<%=avatar%>" class="hero-avatar" alt="Avatar">
        <h1 class="fw-bold">Olá, <%=member.nick%>!</h1>
        <p class="fs-5 opacity-75">Bem-vindo ao painel de gestão do Assistente.</p>

        <div class="hero-search-wrapper">
            <form id="heroSearchForm">
                <input type="text" id="heroSearchInput" class="hero-search-input" placeholder="Pesquisar jogador (ex: DG0837)...">
                <button type="submit" class="hero-search-btn"><i class="fas fa-search"></i></button>
            </form>
        </div>
    </div>
</header>
  
<div class="container">
  
  <div class="row g-4">
    
    <div class="col-md-6 col-lg-3">
        <a href="/mapa" class="feature-card">
            <div class="card-icon"><i class="fa-solid fa-map-location-dot"></i></div>
            <h3 class="card-title">Mapa Mundi</h3>
            <p class="card-desc">Visualize o mundo do Survival Geopolítico, cidades e nações em tempo real.</p>
        </a>
    </div>

    <div class="col-md-6 col-lg-3">
        <a href="/stats/survival" class="feature-card">
            <div class="card-icon"><i class="fa-solid fa-users"></i></div>
            <h3 class="card-title">Online Geo2</h3>
            <p class="card-desc">Veja a lista de todos os jogadores online no servidor atual.</p>
        </a>
    </div>

    <div class="col-md-6 col-lg-3">
        <a href="/stats/topcall" class="feature-card">
            <div class="card-icon"><i class="fa-solid fa-trophy"></i></div>
            <h3 class="card-title">Top Call</h3>
            <p class="card-desc">Estatísticas dos membros da staff com maior tempo em call.</p>
        </a>
    </div>

    <div class="col-md-6 col-lg-3">
        <a href="/dash" class="feature-card">
            <div class="card-icon"><i class="fa-solid fa-sliders"></i></div>
            <h3 class="card-title">Dashboard</h3>
            <p class="card-desc">Gerencie suas configurações e visualize informações da sua conta.</p>
        </a>
    </div>

  </div> <div class="footer-note">
    &copy; <%= new Date().getFullYear() %> Craftsapiens Assistente • Desenvolvido com <i class="fas fa-heart text-danger"></i>
  </div>

</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>

<script>
    // Lógica da Barra de Pesquisa do Hero
    document.getElementById('heroSearchForm').addEventListener('submit', function(e) {
        e.preventDefault();
        const nick = document.getElementById('heroSearchInput').value.trim();
        
        if (nick) {
            // Redireciona para a rota limpa /player/NICK
            window.location.href = '/player/' + encodeURIComponent(nick);
        }
    });
</script>

</body>
</html>