<!DOCTYPE html>
<html>
<head>
    <title>Mapa | Craftsapiens</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
        html, body {
            margin: 0;
            padding: 0;
            height: 100%;
            overflow: hidden;
        }
        iframe {
            width: 100%;
            height: 100%;
            border: none;
            position: fixed;
            top: 0;
            left: 0;
        }
        .search-box {
            position: absolute;
            top: 10px;
            left: 50%;
            transform: translateX(-50%);
            z-index: 10;
            background-color: rgba(0, 0, 0, 0.5);
            padding: 10px;
            border-radius: 5px;
            transition: opacity 0.3s;
        }
        
        .search-box input[type="text"] {
            padding: 5px;
            border-radius: 3px;
            border: 1px solid #ccc;
        }

        .search-box input[type="submit"] {
            background-color: #007bff;
            border: none;
            color: white;
            padding: 5px 10px;
            border-radius: 3px;
            cursor: pointer;
            transition: background-color 0.3s, opacity 0.3s;
        }
        .search-box input[type="submit"]:hover {
            background-color: #0056b3;
        }

        /* Estilos para quando a busca está desativada (Geo1) */
        .search-box input:disabled {
            background-color: #e9ecef;
            color: #6c757d;
            cursor: not-allowed;
            border-color: #d6d8db;
        }
        .search-box input[type="submit"]:disabled {
            background-color: #6c757d;
            opacity: 0.7;
        }
        
        /* Botões do Topo */
        .top-button {
            position: absolute;
            top: 10px;
            z-index: 10;
            background-color: #333;
            border: none;
            border-radius: 3px;
            padding: 8px 12px;
            cursor: pointer;
            color: white;
            font-size: 16px;
            transition: background-color 0.3s;
        }
        .top-button:hover {
            background-color: #555;
        }

        .info-button {
            left: 10px;
        }
        
        .server-button {
            left: 60px;
            background-color: #28a745; /* Verde para Geo2 */
        }
        
        .server-button.geo1-active {
            background-color: #dc3545; /* Vermelho para Geo1 */
        }

        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 20;
            justify-content: center;
            align-items: center;
        }
        .modal-content {
            background: white;
            padding: 20px;
            border-radius: 5px;
            width: 300px;
            text-align: center;
        }
        .modal-content button {
            background-color: #007bff;
            border: none;
            color: white;
            padding: 10px 20px;
            border-radius: 5px;
            cursor: pointer;
        }
        .modal-content button:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body>

    <button class="top-button info-button" title="Informações">
        <i class="fas fa-info-circle"></i>
    </button>

    <button class="top-button server-button" id="serverToggle" title="Alternar entre Geo2 (Atual) e Geo1 (Memorial)">
        <i class="fas fa-globe-americas"></i> <span id="serverLabel">Geo2</span>
    </button>

    <div class="modal" id="infoModal">
        <div class="modal-content">
            <h2><i class="fas fa-info-circle"></i> Informações</h2>
            <p>
                Este é o mapa do servidor Survival Geopolítico da Craftsapiens.
                <br>
                Use o botão no topo para alternar entre o <b>Geo2 (Atual)</b> e o <b>Geo1 (Memorial)</b>.
                <br><br>
                Dica: Caso queira se ocultar do mapa utilize o comando <code>/dynmap hide</code>, para aparecer novamente utilize <code>/dynmap show</code>.
                <br><br>
                <code>
                Criado com <i class="fas fa-heart" style="color: red;"></i> por <a href="https://github.com/davidcanas" target="_blank">DG0837</a>.
            </code>
            </p>
            <button onclick="closeModal()">Fechar</button>
        </div>
    </div>

    <div class="search-box">
        <form action="">
            <input type="text" id="cityName" name="cityName" placeholder="Digite o nome da cidade" >
            <input type="submit" id="searchBtn" value="Procurar">
        </form>
    </div>

    <iframe src="about:blank" id="cityIframe"></iframe>

    <script>
        // Variável de estado para saber qual servidor está ativo
        var currentServer = 'geo2'; // geo2 (padrão) ou geo1

        function updateIframe() {
            var cityName = document.getElementById('cityName').value;
            // Adicionamos o parametro server= na URL
            var iframeSrc = `http://jogar.craftsapiens.com.br:50024/mapa/iframe?cityName=${encodeURIComponent(cityName)}&server=${currentServer}`;
            document.getElementById('cityIframe').src = iframeSrc;
        }

        // Lógica do botão de alternar servidor
        document.getElementById('serverToggle').addEventListener('click', function() {
            var btn = this;
            var label = document.getElementById('serverLabel');
            var cityInput = document.getElementById('cityName');
            var searchBtn = document.getElementById('searchBtn');
            
            if (currentServer === 'geo2') {
                // MUDAR PARA GEO 1 (MEMORIAL)
                currentServer = 'geo1';
                label.innerText = 'Geo1 (Memorial)';
                btn.classList.add('geo1-active');
                
                // Desativar a busca
                cityInput.value = ''; // Limpa o texto
                cityInput.placeholder = 'Busca indisponível no Geo1';
                cityInput.disabled = true;
                searchBtn.disabled = true;
                searchBtn.value = 'X'; // Opcional: muda o texto do botão

            } else {
                // MUDAR PARA GEO 2 (ATUAL)
                currentServer = 'geo2';
                label.innerText = 'Geo2';
                btn.classList.remove('geo1-active');

                // Reativar a busca
                cityInput.placeholder = 'Digite o nome da cidade';
                cityInput.disabled = false;
                searchBtn.disabled = false;
                searchBtn.value = 'Procurar';
            }
            
            // Atualiza o iframe com o novo servidor (e sem nome de cidade se for geo1)
            updateIframe();
        });

        document.querySelector('form').addEventListener('submit', function(event) {
            event.preventDefault();
            // Prevenção extra: não deixa submeter se estiver no geo1
            if (currentServer === 'geo2') {
                updateIframe();
            }
        });

        // Inicializa o mapa
        updateIframe();

        document.querySelector('.info-button').addEventListener('click', function() {
            document.getElementById('infoModal').style.display = 'flex';
        });

        function closeModal() {
            document.getElementById('infoModal').style.display = 'none';
        }
    </script>
</body>
</html>