<!DOCTYPE html>
<html lang="en">
<head>
  <title>Inicio</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #282b30;
      color: #fff;
    }
    .player-card {
      background-color: #333;
      border-radius: 10px;
      padding: 8px;
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: background-color 0.3s ease;
    }
    .player-card:hover {
      background-color: #555;
    }
    .player-card h4 {
      color: #fff;
      margin-bottom: 0;
      font-size: 14px;
      display: flex;
      align-items: center;
    }
    .player-card p {
      margin-bottom: 0;
      font-size: 14px;
      display: flex;
      align-items: center;
    }
    .players-list {
      max-height: 60vh;
      overflow-y: auto;
    }
    .players-title {
      font-size: 20px;
      font-weight: bold;
      color: #fff;
    }
    .heart-icon {
      color: red;
      margin-left: 1px;
    }

    /* Cores dos cargos */
    .tag.reitor { color: #00AAAA; }   /* &3 */
    .tag.dev { color: #55FFFF; }      /* &b */
    .tag.admin { color: #AA0000; }    /* &4 */
    .tag.professor { color: #55FF55; }/* &a */
    .tag.moderador { color: #FF55FF; }/* escolha (pode trocar) */
    .tag.ajuda { color: #FF55AA; }    /* escolha */
    .tag.premium { color: #00AA00; }  /* &2 */
    .tag.vip { color: #FFFF55; }      /* &e */
  </style>
   <%- include('../partials/navbar.ejs') %>
</head>
<body>
  <% include('../partials/navbar.ejs') %>
  <div class="container-fluid p-5 bg-primary text-white text-center">
    <h1>Informações ao vivo do Survival Geopolitico 2 (atualizando a cada 5s)</h1>
  </div>
  
  <div class="container mt-4">
    <div class="row">
        <h3 class="players-title mb-3"><i class="fas fa-user"></i> Lista de jogadores (<%=playerList.length%>)</h3>
        <div class="players-list">
          <% playerList.forEach(function(player) { 
               const role = highestRole(guild, player.username); 
          %>
            <div class="player-card">
              <% if (!role || role.toLowerCase() === "default" || role.toLowerCase() === "alunos") { %>
                <!-- Sem cargo (não mostra tag) -->
                <h4><%= player.nickname %></h4>
              <% } else { %>
                <!-- Com cargo (primeira letra maiúscula e cor) -->
                <h4>
                  <span class="tag <%= role.toLowerCase() %>">[<%= role.charAt(0).toUpperCase() + role.slice(1).toLowerCase() %>]</span> 
                  <%= player.nickname %>
                </h4>
              <% } %>
              <p><%= player.status.health %> <i class="fas fa-heart heart-icon"></i></p>
            </div>
          <% }); %>
      </div>
    </div>
  </div>
</body>
<script>
  setTimeout(function(){
   window.location.reload(1);
}, 5000);
</script>
</html>
