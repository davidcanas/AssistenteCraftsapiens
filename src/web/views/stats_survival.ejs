<!DOCTYPE html>
<html lang="en">
<head>
  <title>Inicio</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <style>
    body {
      background-color: #282b30;
      color: #fff;
    }
    .player-card {
      background-color: #333;
      border-radius: 10px;
      padding: 8px; /* Reduzindo o preenchimento do card */
      margin-bottom: 10px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      transition: background-color 0.3s ease;
    }
    .player-card:hover {
      background-color: #555;
    }
    .player-card h4 {
      color: #fff;
      margin-bottom: 0; /* Removendo a margem inferior do nome do jogador */
      font-size: 14px; /* Reduzindo o tamanho do nome do jogador */
      display: flex;
      align-items: center; /* Alinhar verticalmente o texto e a vida */
    }
    .player-card p {
      margin-bottom: 0; /* Removendo a margem inferior da vida do jogador */
      font-size: 14px; /* Mantendo o mesmo tamanho da fonte */
      display: flex;
      align-items: center; /* Alinhar verticalmente o texto e a vida */
    }
    .players-list {
      max-height: 60vh;
      overflow-y: auto;
    }
    .players-title {
      font-size: 20px;
      font-weight: bold;
      color: #fff;
    }
    .heart-icon {
      color: red;
      margin-left: 1px;
    }
  </style>
   <%- include('../partials/navbar.ejs') %>
</head>
<body>
  <% include('../partials/navbar.ejs') %>
  <div class="container-fluid p-5 bg-primary text-white text-center">
    <h1>Informações ao vivo do Survival Geopolitico (atualizando a cada 5s)</h1>

  </div>
  
  <div class="container mt-4">
    <div class="row">
      
        <h3 class="players-title mb-3"><i class="fas fa-user"></i> Lista de jogadores (<%=playerList.length%>)</h3>
        <div class="players-list">
          <% playerList.forEach(function(player) { %>
            <div class="player-card">
              <% if (!highestRole(guild, player.name) || highestRole(guild, player.name) == "Alunos") { %>
                <h4><%= player.name %></h4>
              <% } else { %>
              <h4><span class="tag <%=highestRole(guild, player.name).toLowerCase()%>">[<%=highestRole(guild, player.name)%>]</span> <%= player.name %></h4>
              <% } %>
              <p><%= player.health %> <i class="fas fa-heart heart-icon"></i></p>
            </div>
          <% }); %>
      </div>
    </div>
  </div>
</body>
<script>
  setTimeout(function(){
   window.location.reload(1);
}, 5000);

</script>
</html>
